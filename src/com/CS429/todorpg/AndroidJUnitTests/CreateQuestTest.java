package com.CS429.todorpg.AndroidJUnitTests;
/*
import com.CS429.todorpg.QuestCreation;
import com.CS429.todorpg.R;

import android.test.ActivityInstrumentationTestCase2;
import android.util.Log;
import android.widget.EditText;
import android.widget.ListView;
import android.widget.Spinner;

public class CreateQuestTest extends
ActivityInstrumentationTestCase2<QuestCreation> {

	private QuestCreation questCreation;
	private EditText title, duration, description, newMilestone;
	private ListView milestones;
	private Spinner location_spinner;
	
	public CreateQuestTest() {
		super(QuestCreation.class);
		// TODO Auto-generated constructor stub
	}
	
	@Override
	protected void setUp() throws Exception {
		super.setUp();
		questCreation = getActivity();
		milestones = (ListView) findViewById(R.id.quest_milestones);
		newMilestone = (EditText)findViewById(R.id.creation_quest_milestone);
		duration = (EditText) findViewById(R.id.creation_quest_duration);
		description = (EditText) findViewById(R.id.creation_quest_description);
		title = (EditText) findViewById(R.id.creation_quest_title);
		location_spinner = (Spinner)findViewById(R.id.creation_quest_location);
	}
	
	public void testPreconditions() {
		assertNotNull("myCreateProfileActivityTest is null", questCreation);
		assertNotNull("title is null", title);
		assertNotNull("description is null", description);
		assertNotNull("duartion is null", duration);
		assertEquals("Choose one", location_spinner.getSelectedItem().toString());
	}
	
	public void testValidation() {
		assertFalse(questCreation.validate());
	}
	
}
*/
